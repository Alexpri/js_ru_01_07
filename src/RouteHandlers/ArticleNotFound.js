import React, { Component, PropTypes } from 'react'

class ArticleNotFound extends Component {
    static propTypes = {

    };

    render() {
        return (
        	<div>
        		<h3>Article with id: not found</h3>
        	</div>
        )
    }
}

export default ArticleNotFound